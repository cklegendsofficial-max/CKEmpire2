apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: ck-empire
  labels:
    app: ck-empire-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ck-empire-backend 