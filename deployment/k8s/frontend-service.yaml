apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: ck-empire
  labels:
    app: ck-empire-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: ck-empire-frontend 